// Re-export from the shared installer with rules-specific defaults
export { installAsset, uninstallAsset, sanitizeName, getCanonicalDir, getAgentDir, type InstallMode, type InstallResult } from '../skills/installer.js';
